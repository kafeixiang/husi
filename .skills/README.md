# Husi Project skills

Link / copy this directory to `.claude/.skills` and/or `.codex/.skills`

Run on project root:

```shell
mkdir -p .claude/ .codex/
ln -s ../.skills .claude/
ln -s ../.skills .codex/
```